@model IList<AssetDTO>

@{
    ViewBag.Title = "Assets";
}

@*<partial name="AddAsset" />*@

<div class="form-row mb-2">
    <div class="col">
        <div class="btn-group" role="group">
            <button type="button" class="btn btn-primary">Create</button>
            <div class="btn-group" role="group">
                <button id="btnCreateWithType" type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown"></button>
                <div class="dropdown-menu" aria-labelledby="btnCreateWithType">
                    <a class="dropdown-item" href="#">Dropdown link</a>
                    <a class="dropdown-item" href="#">Dropdown link</a>
                </div>
            </div>
        </div>
        <div class="col"></div>
    </div>
</div>

<partial name="AssetList" model="Model" />

@section Scripts{
    <script src="~/lib/ajax-unobstrusive/dist/jquery.unobtrusive-ajax.js" type="text/javascript"></script>
}